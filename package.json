{
  "name": "desafio-cypress-QA",
  "version": "1.0.0",
  "description": "Desafio Cypress QA - automação de testes para Netshoes",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "test": "npm run cy:run",
    "allure:generate": "allure generate allure-results --clean -o allure-report",
    "allure:report": "npm run allure:generate && allure open allure-report",
    "report": "npm run test && npm run allure:report"
  },
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.41.2",
    "allure-commandline": "^2.34.1",
    "cypress": "^15.7.1",
    "cypress-xpath": "^1.6.2"
  }
}
